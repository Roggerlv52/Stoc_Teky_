<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/gray</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/gray_dark</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorOnPrimary</item>
        <!-- Customize your theme here. -->
        <item name="colorTextLogin">@color/blue</item>
        <item name="colorTextHint">@color/black</item>
        <item name="icIconColor">@color/gray_dark</item>
        <item name="colorToolbar">@color/orange</item>
        <item name="colorScreen">@color/white</item>
        <item name="colorButtonBackground">@color/orange</item>
        <item name="colorDrawerBackground">@color/white</item>
        <!-- Define as cores do gradiente do menu lateral -->
        <item name="center_color">#FF9800</item>
        <item name="start_color">#FF9800</item>
        <item name="end_color">#E7BE81</item>
    </style>

    <style name="Theme.MyApplication.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MyApplication.AppBarOverlay" parent="Theme.AppCompat.DayNight">
        <item name="android:backgroundTint">@color/orange</item>
    </style>
    <style name="Theme.MyApplication.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>